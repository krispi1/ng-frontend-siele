function pagination(numberOfProducts){
    for(let i = 0; i < products.length; i++) {
        /**
         *ALGORITHM 
         * 
         * grab all products
         * divide by number of products per page
         * 
         * generate pages equal to value of quotient
         * 
         * 
         *  
         **/ 
    }
}